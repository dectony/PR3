.container
    table.table.table-hover.table-striped.table-condensed
        thead
            tr
                th Title
                th Published Date
                th Duration
        tbody
        tr(ng-repeat="auction in auctions")
            td {{auction.title}}
            td {{auction.published | date}}
            td {{auction.duration | date}}